<div class="container">
 <div class="row">
  <div class="col-sm-3 col-md-3 text-center">
    <h2 class="targetname"><%= link_to "#{@task.target.name}", targets_path %></h2>
    <h2 class="taskname"><%= link_to "#{@task.name}", target_tasks_path %></h2>
    <button type="button" class="btn btn-default td-add-button"><div><%= link_to "Todoを追加する", new_target_task_todo_path(@task.target_id, @task.id) %></div></button>
    <div></div>
  </div>
  <div class="col-sm-9 col-md-9 tsk-fld">
    <div><%= render :partial => "todo", :collection => @todos.order(created_at: :DESC) %></div>
    </div>
  </div>
 </div>
</div>